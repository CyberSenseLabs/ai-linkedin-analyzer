# Raw LinkedIn data exports — never commit these
*LinkedInDataExport*
*.zip
linkedin_export/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Live enrichment output (contains scraped third-party profile data)
data/enrichment.json

# Extracted per-company connection data (contains your real connections)
data/company_people.json
.vercel

# Next.js / Node (SaaS app)
node_modules/
.next/
out/
next-env.d.ts
.env.local
.env*.local
*.tsbuildinfo

# Allow the synthetic test export (real exports stay ignored via *.zip above)
!sample_data/synthetic_linkedin_export.zip
